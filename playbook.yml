---
- hosts: all
  become: yes
  tasks:
    - name: make sure that we can connect to the machine
      ping:

    - name: ensure aptitude is installed
      apt:
        name:
          - aptitude
        state: present

